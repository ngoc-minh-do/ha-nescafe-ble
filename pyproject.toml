[project]
name = "ha-nescafe-ble"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bleak>=3.0.2",
]

[project.scripts]
brewctl = "brewctl:main"
